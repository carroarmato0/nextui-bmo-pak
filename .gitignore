# Build artifacts
bin/
dist/
lib/
# ...but keep the tracked shell helpers under scripts/lib/ (not build output).
!scripts/lib/
# Command binaries built into the repo root (anchored so the cmd/ source dirs stay tracked)
/generate-audio
/bmo-pak
.go_cache/
.go_cache.docker-polluted/
*.pak
*.pak.zip
coverage.out
*.prof
*.pprof
*.trace

# Graphify outputs (generated knowledge graph and caches)
graphify-out/

# Local debugging / profiling markers
.profile-flags

# Git worktrees
.worktrees/

# Local environment / API keys (never commit)
.env

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Local AI tooling state (per-machine, not shared)
.superpowers/
# Ignore .claude/ per-machine state, but share project skills in git.
.claude/*
!.claude/skills/
.headroom/
.remember/
.serena/
debug-profiles/

# P2.0 wake-word spike artifacts (large binaries; re-download via findings doc)
spike-artifacts/
wakeword-spike-arm64

# Fetched wake-word assets (onnxruntime .so + openWakeWord models)
third_party/wakeword/
